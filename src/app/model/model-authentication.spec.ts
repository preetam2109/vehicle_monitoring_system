import { ModelAuthentication } from './model-authentication';

describe('ModelAuthentication', () => {
  it('should create an instance', () => {
    expect(new ModelAuthentication()).toBeTruthy();
  });
});
