import { TblTop12RestaurantsList } from './tbl-top-12-restaurants-list';

describe('TblTop12RestaurantsList', () => {
  it('should create an instance', () => {
    expect(new TblTop12RestaurantsList()).toBeTruthy();
  });
});
