import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.instamessanger.app',
  appName: 'instamessanger',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
